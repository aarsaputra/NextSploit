# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# NextSploit generated scan reports (runtime output, never committed)
reports/
scan*.json
report.json
report.html
report.md
*.sarif

# Build / update cache
.update_cache.json

# Dev scratch space
scratch/
!requirements.txt

# Local rule testing workspace (personal, not for repo)
knowledge/rules/local/

# Runtime state — per-user toggle, generated at runtime
nextsploit/policies/active_plugins.json

# AI Agent context (local only)
.agent/

# Raw development notes (not part of the framework)
research.md

# Legacy v3 entrypoint (kept locally for reference only)
nextsploit_legacy.py

# Local vulnerable lab environment (Docker Compose, not for repo)
vuln-lab/

# Virtual Environments
venv/
env/
ENV/
.venv/
env.bak/
venv.bak/

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Secrets / Environment Variables
.env
.env.local
.env.*.local
*.pem
*.key
credentials.json
secrets.json
